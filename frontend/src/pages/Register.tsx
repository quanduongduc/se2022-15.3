import React, { ReactElement } from 'react';

const RegisterPage = (): ReactElement => {
    return (
        <>
            <h1>Register Page</h1>
        </>
    );
};

export default RegisterPage;
