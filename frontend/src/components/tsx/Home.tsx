import { ReactElement } from 'react';
import '../css/home.css';

const Home = (): ReactElement => {
    return (
        <>
            <div className="home-wrapper"></div>
        </>
    );
};

export default Home;
